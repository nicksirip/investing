input rsiLength = 14;
input overbought = 65.0;
input oversold = 35.0;

def rsiVal = RSI(length = rsiLength);

plot data = rsiVal;

data.AssignValueColor(
    if rsiVal >= overbought then Color.RED
    else if rsiVal <= oversold then Color.GREEN
    else Color.CURRENT
);
